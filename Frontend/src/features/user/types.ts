export interface UserState {
  userId: string;
  userName: string;
  email: string;
  role: string;
  profileUrl: string;
  token: string;
}
export interface LoginPayload {
  userId: string;
  userName: string;
  email: string;
  role: string;
  profileUrl: string;
  token: string;
}
