import { Typography } from "@mui/material";
import { FC } from "react";

const NotSetYet: FC = () => {
  return (
    <Typography variant="body2" color="GrayText" sx={{ fontStyle: "italic" }}>
      not set yet
    </Typography>
  );
};

export default NotSetYet;
